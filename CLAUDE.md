# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`diff-voyn` — a research codebase for language identification of Voynich-like ciphertext via trial decipherment. A multilingual masked (absorbing-state) discrete diffusion model over characters serves as the likelihood instrument; differentiable cipher decoding heads are optimized against it. Target output is a ranked (cipher system × plaintext language) table for the Voynich Manuscript.

### Current status (2026-09-03) — read this first

**Reading rule.** Every document and memory in this project is a dated record; the first mention of a number or technique in a file is *not* its current value. `docs/project_status.md` is the single current-status page and the arbiter when records disagree (its §5 lists every superseded statement and the canonical reading); every file in `docs/` carries a banner naming the period it records. When you quote a performance figure, name the solver stage and date it.

- **Verdict: the manuscript ABSTAINS under every head and solver tried** — Phase 6 (2026-08-24) 0/87 cells; alternating loop (08-26) 72/72 NOISE; wildcard→anneal on the 12 word-homophonic cells (08-29) 24/24 NOISE; `d5b20` decoder (08-31) 8/8 NOISE. The language ranking among the non-decipherments is noise. One structural result stands: a Naibbe-shaped token grammar compresses Currier A (2.09 vs 2.18 / 2.12 vs 2.23 bits/symbol), not B — description length, not decipherment.
- **Solver of record (word-homophonic cells):** hapax-wildcard objective → anneal re-admission + post-all loop, patience ≥ 10 (`scripts/altloop_vms.py --wild`, then `--wild-anneal 0,40 --patience 10 --rounds 80 --start-from <wild tag>`; `docs/alt_loop_plan.md` §8.4–8.6, §10). **Findability wall by stage:** plain SA ≥ 8 tokens/type → posterior re-seeding 6.6 → wildcard→anneal **≈ 4** (A-like 4.1–4.4 solved at SER 0.05 de / 0.07 la / 0.12 it — German and Latin *called* by the frozen judge, Italian not; ~3.5 fails; a 48-unit key at ~4 fails). **Currier B (4.6) is inside the solvable regime and still NOISE → its abstention is not a search-budget failure; Currier A (3.0) is below the wall** (the n-gram optimum is not the truth there). Never quote "≥ 8" for the current pipeline. Windowing lowers tokens/type (1.8–2.0 per 1024-token window) and was rejected 2026-09-01. Synthetic "A-like/B-like" cells are 4.1–4.4 / 5.5–5.7 tokens/type with ~45 % hapax types — not the manuscript's 3.0 / 4.6 with 69–75 %.
- **Judge of record:** frozen `vms/apply.py::ABSTAIN_RULE` (plain ≤ 3.0 bits/char AND structure margin ≥ 1.5). It calls only near-perfect decodes (anneal finals: de SER 0.05 → margin 2.13, la 0.07 → 1.70, it 0.12 → 1.39 not called; Italian A-like truth itself is 1.56). Judge inside the loop's acceptance rule: tested 2026-08-28, **no gain**. Strict-gibberish ceiling **1.40** (letter-shuffled-source voynichesque twins, 08-31; 9/9 abstain) → the corridor safe against a strict negative is ≈ 1.41–1.48; the Phase-6 real-text voynichesque band 0.92–1.51 is content-inflated (homophonic Δ −0.27, 27/27 pairs) and is a wrong-hypothesis control, so the Phase-6 P0 "FAIL" (0.89) was a mis-specified test on a non-negative (verdict left as recorded). Candidate second criteria — rare-aware margin, exact ≥ 7-letter lexical density, learned LID/posterior LR (`docs/judge_alternatives.md`) — call SER 0.12–0.20 on synthetics but are **not pre-registered, not implemented, not adopted**. Polygraphia produced the pipeline's only false CALLS (1.76–1.96 at SER 0.74–0.77) and is quarantined (below).
- **Controls:** manuscript-shaped wordhom battery (08-29/30): 12 negatives + 6 cross-language NOISE ≤ 0.48; clean de/la positives called; `nodouble` costs nothing; `revdouble` German still called; dirty-10 % truth ceilings 1.54 / 1.06 / 0.91 (B shape 1.52 / 1.23 / 0.93 — ceilings do not depend on shape). **Dirty positives at B shape (2026-09-03, `docs/alt_loop_plan.md` §10.6):** dirty-5 % German **called on both seeds** (SER 0.085, margin 1.69–1.70), the first replicated dirty call; dirty 10 % uncalled everywhere; Latin s05 drew a 3.00-bpc text (uncallable at truth 1.24 — re-draw before quoting a Latin power); Italian uncallable at truth though its search reaches SER 0.22. Latin held-out doc 0 is a 4.66-bpc pharmacopoeia (battery sampler resamples > 3.6 bpc) — check Latin plaintext bpc before calling Latin "hard". Naibbe real-vs-shuffled twins at 3.4 tokens/type are indistinguishable (zero content below the wall).
- **Evidence odds (2026-09-02, `docs/bayesian_perspective_review.md` §5, `scripts/evidence_odds.py`):** a likelihood-ratio reading of the abstention from the recorded control piles (not pre-registered, not a call rule). Clean German/Latin word-homophonic cipher: LR < 1/400 on every A and B cell, power 1.0. Dirty-10 % cipher: LR 1/2–1/4, power 0. Italian: uncallable even at truth. After the B-shape dirty run (09-03) the German dirty-5 % pile is n 4 / power 0.75 and the B cells sit at 1 : 83–100 against a 5 %-noisy German cipher. Quote the verdict as "B excludes the clean de/la hypothesis and a 5 %-noisy German one at ≈ 1 : 90; 10 %-noisy, noisy-Latin and Italian variants are not excluded; A is untested on the noisy variants at its own shape." Proposals R2–R6 (posterior-predictive judging of rare entries, weighted key population, tied key prior for A, marginal-likelihood content test, uncertainty-aware decisions) are listed in `docs/project_status.md` §6 and have not been run. R2 is **pre-registered** (`docs/judge_posterior_predictive.md`, readings fixed in its §4; `scripts/judge_posterior_predictive.py`) and its launch waits for the user's say-so; the B-shape dirty positives were run 2026-09-02 → 03 (above).
- **Instrument (unchanged since Phase 5):** evaluator `phase_c-85m-seed0/ckpt_final.pt`; calibration `v3-phase_c-ro`, report-only; budget 64 × 4 seeds; cells ranked on the MDL total per ciphertext symbol; choice term never inside a polish or loop (code raises). Borg's `elbo_polish` degradation was the choice term in the objective (2026-08-25), not winner's curse; ELBO-only polish holds Borg.
- **Side study (2026-09-02/03, `docs/doubleton_gaps.md`, off the critical path):** rare-word locality in the manuscript is ≈ 1/10 of prose (2.5–4th percentile of 148 known windows); conjugate leaves of one bifolium share rare material more than nested neighbours (glyph n-grams z +3 to +5, hand-controlled; 0/5 000 even-spread draws at n ≥ 7) — the pattern Davis's stacked-bifolia proposal predicts, but composition-unit vs work-unit is indistinguishable; stacked-order windows do not raise tokens/type, whole-manuscript sheet re-ordering gains only the noise-ceiling amount, rare types are not contained by sheet. One-quire seriation with a burst-scaled rare-material metric (§13–15): quire T's six sheets chain as 1-6-5-4-2-3 (31/32 cells, p ≤ 0.02 vs shuffled contents and vs geometry), quire C 1-4-3-2, outermost-sheet-next-to-innermost in T, C and M — *adjacency only*: reading direction is not established (§16, sheet-reversal ≠ page-reversal) and prose bursts carry no time arrow to exploit (§18). Alternative nesting patterns (§19, 2026-09-03): every reordered, sub-gathered or mixed nesting of a quire's sheets (up to 23 040 candidates) is no better than the stacked optimum beyond the selection null, and the best fully nested order ranks far down (T 5 007–10 535 of 23 040) — the stacked-over-nested result does not rest on the binding being the only nested order tried; use L1 for that class question (burst-family metrics are biased toward stacked patterns there).
- **Open:** control 6a/6b paused since 2026-08-24 (6b rung 1 done, no write-up); task 4.7 paused; race-polish full study not run; Borg DECODE-aligned + BnF fr2988 transcriptions; EVA tokenization sweep; pre-registering any second judge criterion; anneal budget / second seeds on borderline positives; a compact key rule for Currier A. Full register: `docs/project_status.md` §6.

### Dated record, 2026-08-18 → 2026-08-26 (historical — superseded sentences are annotated in brackets; `docs/project_status.md` §5 arbitrates)

**Current state: Phase 6 complete — the VMS application is done (2026-08-24). Headline: the manuscript ABSTAINS. Across 87 (cipher × language × window × presentation) cells — four heads, three languages, Currier A and B, two EVA transcriptions plus Boxer's glyph transcription — no cell is a language-like decipherment. The decisive statistic is the per-instance **structure margin** (bits of a letter-shuffled copy minus bits of the decode, own condition): the VMS spans 0.04–1.25 bits/char while nine true decipherments through the identical pipeline span 1.49–2.48, `voynichesque` gibberish 0.92–1.51 and out-of-inventory contamination 0.60–1.43 — any threshold in 1.26–1.48 abstains on the whole manuscript and on no positive control *[2026-08-31: the real-text voynichesque band is content-inflated (homophonic Δ −0.27, 27/27 pairs) — a wrong-hypothesis control, not a strict negative; strict letter-shuffled twins peak at 1.40, so the corridor also safe against a strict negative is ≈ 1.41–1.48]*, so the verdict does not rest on the frozen rule (`apply.ABSTAIN_RULE`: plaintext ≤ 3.0 bits/char AND margin ≥ 1.5, fixed before any manuscript number was read). The language ranking among those non-decipherments is noise and is reported as such (within-head margins 0.001–0.083 bits/ciphertext symbol vs calibration uncertainty 0.067–0.193; heads/dialects/transcriptions disagree while the replicate flip-rate is 0 on 80/87 cells). One structural result survives: a Naibbe-shaped unigram/bigram token grammar describes **Currier A** below the stream's own held-out bigram cross-entropy (2.09 vs 2.18 and 2.12 vs 2.23 bits/symbol, both transcriptions) but not Currier B — a description-length statement about word structure, not a decipherment (its decode sits at 3.5 bits/char). Acceptance (`scripts/phase6_check.py` → `runs/phase6_report.json`): **FAIL on one P0 sub-criterion** — voynichesque abstention 0.89 against > 0.95 (one instance at margin 1.51; symmetrically one true decipherment at 1.49 was falsely abstained) *[2026-08-31: the 1.51 instance deflates to 1.24 without content and the strict-negative twins abstain 9/9 — the bar was tested on a non-negative; verdict left as recorded]* — with 6.1/6.2/6.4/6.5 and the freeze discipline all PASS. Anchors: Zodiac-408 SER 0.0098 (PASS, n-gram tier only — English is outside the inventory), Borg ranked **Latin** at 0.250 bits/symbol (the largest margin in the phase) but SER 0.129 n-gram / 0.226 final against the ≤ 0.041 target, which is **not** like-for-like (the published plaintext is a corrected/expanded edition, and we drop glyph types with < 20 occurrences); BnF fr2988 unavailable. **Read `docs/phase6_status.md` and `docs/phase6_writeup.md` first** for the Phase-6 verdict *(and `docs/project_status.md` for the current position)*. Post-Phase-6 probe (2026-08-24, `docs/confidence_mask_probe.md` §9): confidence-masked judging (blank low-confidence letters as `MASK`) was pre-registered, run, and **not adopted** — n-gram key sensitivity is not a language-fair predictor of wrong symbols (E1 fail), and every symbol-level mask *shrinks* the structure-margin gap (oracle ceiling +0.06 vs required +0.15) because the margin is context-limited; only the oracle raw score improves (language recovery 0.58 → 0.94 at 65 % wrong), which needs a far better confidence signal than exists. Code `heads/{masked_bits,confidence}.py`, `scripts/confidence_probe.py`, artifacts `analysis/confidence_probe/`. **Racing polish (2026-08-25, `docs/race_polish_plan.md` §7, proof-of-life only — full study NOT run)**: `ladder.race_polish` (adaptive-budget successive-halving over the paired-difference neighbourhood, fresh masks per stage, Bonferroni commit; `two_tier.paired_bits_strata` exposes per-stratum values) is a sound selection rule — null controls unmoved, corruptions repaired, rung-2 picks improved beyond greedy — but Borg's degradation turned out to be mostly an **objective** problem, not winner's curse: the frozen judge dislikes `e`→`z` (+0.087 bits/char) while the homophonic choice-bits term rewards it (−0.243) because it makes a frequent symbol the sole homophone of a spare letter; race on the MDL objective still degrades Borg (0.120→0.225, greedy 2-sweep 0.310), race on the **ELBO alone holds it** (0.1195→0.1194). Greedy on the ELBO alone also holds Borg (0.1198), so the objective is the whole story there. **Code defaults now enforce this**: `elbo_polish`/`race_polish` raise unless `choice_term_in_polish=True`, and `vms/apply.py` (`polish_choice_term=False`), `rung2_diffusion.py` and `control6b_pooled_search.py` (`--polish-choice-term`) polish on the ELBO alone — pass the opt-in only to reproduce the recorded Phase-5/6 numbers. Keep the choice term for shortlist/cell ranking (MDL total), never inside a polish or any future alternating n-gram ↔ diffusion loop. Two findings change how the machinery should be reused: (a) **`elbo_polish` does not transfer to large move neighbourhoods** — on Borg (55 symbols, 4000-char window) it was accepted 6/6 and degraded the decipherment every time (median page SER 0.110 → 0.217, 25/55 symbols reassigned) because each sweep takes an argmin over ~1375 noisy budget-8 estimates (winner's curse) *[re-diagnosed 2026-08-25, see the racing-polish sentence above: mostly the choice term in the objective; ELBO-only polish holds Borg]* while the single budget-64 confirmation compares only the final map against the start; fix with per-move full-budget confirmation or a bias-corrected selection rule before reuse (it did *not* cause the VMS abstention: pre-polish margins 0.83–0.84 vs post-polish 1.05–1.11); (b) the **arithmetic head cannot run its own way on the manuscript** — no admissible canonical glyph order exists on the Boxer stream (every LOP optimum forces length-1 tokens), i.e. the sum-to-target cipher's sorted-token signature is absent, so `ArithmeticHead.solve_segmented` runs the lattice on the observed word boundaries instead. Phase-6 code: `diff_voyn/vms/{presentations,apply,controls}.py`; scripts `vms_apply.py` (prepare/solve/score/report), `vms_controls.py`, `anchors.py`, `phase6_analysis.py` (6.5), `phase6_samples.py`, `phase6_check.py`, `fairness_audit.py --phase-tag phase6` (6.4); tests `tests/test_phase6.py`; artifacts `DATA_ROOT/analysis/phase6/`. Presentations are frozen with their coverage (EVA characters 20 symbols IT2a / 23 RF1b; Naibbe-parseable words 65–78% of characters; Boxer glyph top-16 for rung 4, top-20 for rungs 1–2), uncovered ciphertext symbols are charged at the stream's own best held-out n-gram cross-entropy, and the cross-head ranking is the MDL total per **all** ciphertext symbols. Open carry-overs *(status 2026-09-01 in brackets)*: fix the outer tier before reuse *[resolved 2026-08-25 as the objective fix; race rule exists, full study not run]*; obtain the DECODE-aligned Borg and BnF fr2988 transcriptions *[open]*; enlarge the control battery around the margin's overlap band *[done 2026-08-29/31: wordhom battery + strict-negative twins]*; sweep EVA tokenizations as the sensitivity study the abstention deserves *[open]*; task 4.7 (25M seed replication, P2) is still **paused at resumable checkpoints** (`docs/phase4_status.md` §4.7) *[still paused]*. **Alternating loop on the manuscript (2026-08-25/26, `docs/altloop_vms_plan.md` §12, `scripts/altloop_vms.py`, artifacts `analysis/altloop_vms/`)**: 72 cells (wordhom 12, homophonic 30, sub1to1 30) × arms `none`/`rand`/`psamp` with the §5 tiers fixed beforehand — **every cell NOISE**, no round reached NOTABLE (psamp margins 0.36–1.05 vs the 1.25 ceiling), 62/72 cells end on the identical key on all three arms, the only accepted rounds are n-gram refits that *lower* the margin; the loop starts from the solves' n-gram MDL pick because the Phase-6 ELBO-polished key sits ~6 500 nats below the n-gram optimum (the judge's and the n-gram's preferred keys are far apart on the manuscript). Method blind at 3.0–4.6 tokens/type as the plan warned; abstention stands; option B (judge in the acceptance rule, manuscript-shaped synthetic battery) is the only stronger path. *[Superseded: judge-in-acceptance tested 2026-08-28 — no gain; the battery ran 2026-08-29/30; what moved the wall was the hapax-wildcard objective + anneal (≈ 4 tokens/type, 08-27/28), run on the manuscript 2026-08-29: 24/24 NOISE — see Current status.]* **Alternating n-gram ↔ diffusion loop, proof of life (2026-08-25, `docs/alt_loop_plan.md` §7)**: the denoiser's per-position letter posterior (`heads/posterior.py`: decode under the current key, mask 30 %, one forward pass, sum log-posteriors per cipher symbol) used as a *proposal* — re-seed the symbols whose posterior disagrees with the key, short n-gram SA, accept only if the **n-gram** objective improves (`heads/altloop.py::alternate`, `scripts/altloop_pol.py`, artifacts `analysis/altloop/`) — escapes n-gram local optima the search cannot leave on its own: under a cold SA (0/24 escapes alone) posterior re-seeding escapes 10/24 rung-2 traps vs 6/24 for a same-size random kick, and takes the two word-homophonic 6.6-tokens-per-type cells the wordhom study recorded as unsolvable from SER 0.42 to 0.026/0.047 (the n-gram objective's own optimum near truth). Limits: the argmax proposer is deterministic and stalls on deep starts (use `posterior_sample`); objective traps (rung-2 german t0 0.240, latin t5 whose true key is 300 nats below the pentagram optimum) resist every proposer and need the judge in the acceptance rule (choice term off) — not done; no VMS cell was run. *[Superseded: german t0 is a search trap (the rand-k2 arm opens it, `alt_loop_plan.md` §9.1); judge-in-acceptance tested 2026-08-28 with no gain; VMS cells run 2026-08-26 (72), 08-29 (24), 08-31 (8) — all NOISE.]* **Post-Phase-6 word-homophonic study (2026-08-24/25, `docs/wordhom_study.md`)**: Boxer's hypothesis proper — word tokens as homophones for letters + the language's top-5 *doubled letters* (ss/ll/tt, the ß analogy; NOT frequent bigrams — that definition fails the doubling control) with the repeat rule — implemented without the arithmetic as `diff_voyn/heads/wordhom.py` (+ `wordtypes_presentation`, `build_wordhom_controls`, the `WORDHOM` branch of `vms/apply.py`, `scripts/wordhom_study.py`, artifacts `analysis/wordhom/`). Result: the hypothesis passes the doubling-rate check (Latin/German 9.5–10.3 doubled units per 1000 vs manuscript token repeats 7–9), but an unstructured word-level homophonic key is only findable **by plain SA** at ≥ 8 tokens per type (perfect recovery there, SER ≥ 0.57 at ≤ 5.7) *[the solver of record, wildcard→anneal, clears ≈ 4 — see Current status]* and the manuscript has 3.0 (A) / 4.6 (B); every manuscript cell abstains (plain 3.31–3.71 bits/char, margin 0.19–0.50, MDL total 2.45–2.56 ABOVE the no-cipher baseline 2.09–2.43) with numbers identical to a true cipher of the same shape, so the abstention is correct but uninformative (the same pipeline recovers and *calls* German at 8.3 tokens/type: SER 0.008, margin 2.27) — some compact key rule (what the arithmetic was) is what would make the hypothesis testable. *[2026-09-01: "uninformative" now holds for Currier A only; Currier B sits inside the current solver's regime and is still NOISE.]* Also: top-K types with gaps is unusable (the true decode of a 69%-covered stream costs 5.7 bits/char), so the head maps every type. Phase 5 (G5 passed 2026-08-23): cipher heads on the frozen diffusion evaluator — rung 1 SER 0.0016 / language recovery 99.4% at ≥200 chars; rung 2 Zodiac-408-class 17/18 instances ≤1.9% SER, median 0, language 18/18 by MDL total; rung 3 Naibbe occurrence-weighted letter-map accuracy 0.998, language 12/12; rung 4 language 7/9, family 8/9; cross-head MDL picks the true cipher class 24/24. Its findings still govern: the outer tier's working form is *discrete* (`ladder.elbo_polish`, but see the scale caveat above), expected-embedding gradient refinement never moves a key usefully, any density judge prefers degenerate verbose decodes so **every (cipher × language) cell is ranked on the MDL total per ciphertext symbol** (`heads/scale.py`, `two_tier.py`) and never on the pure ELBO for a verbose head, the 2N NULL frame costs ~0.5 bits/char so collapsed hard decodes are scored on the plain path, Latin is the hard language for the n-gram inner search at every rung *(check plaintext bpc first — Latin held-out doc 0 is a 4.66-bpc pharmacopoeia, 10 % of sampling weight; 2026-08-30)*, and the ELBO is the worse judge below ~100 chars. Frozen evaluator: `DATA_ROOT/runs/phase_c-85m-seed0/ckpt_final.pt` (sha256 in `analysis/phase5/evaluator_freeze.json`), calibration `v3-phase_c-ro`. Phase-5 code: `diff_voyn/heads/{diffusion_eval,two_tier,scale,ladder}.py`, `NgramEvaluator.viterbi_segmental`, `NaibbeBlockHead.decode/polish/refine_frame`, `HomophonicHead.polish_pairs`; scripts `phase5_freeze.py`, `rung{1,2,3,4}_diffusion.py`, `crosshead_scale.py`, `g5_check.py`, R3 probes `r3_*.py`; artifacts `DATA_ROOT/analysis/phase5/`. Phase 4 (G4 passed 2026-08-22): joint Phase-C 85M, synthetic 1:1 recovery 98.9% language / 99.1% family at ≥200 chars; task 4.7 (25M seed replication, P2) is **paused at resumable checkpoints** — resume per `docs/phase4_status.md` §4.7. Phase-4 code: `diff_voyn/model/lid_head.py`, `diff_voyn/data/abstain.py`, `Backbone.hidden()`, `load_lid_head`; scripts `train_lid_head.py`, `lid_eval.py`, `train.py --phase phase_c`, `head_calibration.py`, `seed_replication.py`, `g4_check.py`; the head is a short-text cross-check, not a wrong-key abstention instrument (`docs/phase4_status.md`). Phase 3 (G3 passed 2026-08-21): calibration offsets vs the multilingual AR reference v3 are *measured and stored but not subtracted* (`policy: report-only`) — applying any AR-gap table breaks same-text comparisons and drops recovery to ~70% (the Phase-3 finding, an escalated §5b.3 design change); measured offsets are the systematic uncertainty of a ranking margin (`CalibrationTable.margin_uncertainty_bits`). Scoring budget: 64 stratified draws (× 4 replicate seeds for a flip-rate). `docs/phase3_status.md` and `docs/phase3_fairness_audit.md` hold the Phase-3 record.** Phase-3 code: `diff_voyn/metrology/` (`scoring.py` — `score_conditions`, CRN harness; `calibration.py` — `CalibrationTable`, `calibrate_bits` the single application point); scripts `crn_check.py`, `sample_budget.py` (`--restat`), `score_documents.py`, `calibrate.py` (`--nelbo-from`, `--derive-report-only`), `fairness_audit.py`, `language_recovery.py` (`--stage solve|score|report`), `g3_check.py`; AR reference v3 via `train_ar_reference.py --multilingual`. Phase-2 code: `diff_voyn/data/noise.py` (wrong-key / Naibbe-parse / transcription generators + 2N-slot NULL frame, `NoiseConfig` mix), `scripts/robustness_curve.py` (severity sweeps, `--restat`), `scripts/g2_check.py`; side study `scripts/ngram_robustness.py` + `docs/ngram_judge_robustness.md` (CH.0 n-gram judges on the same noised windows: no saturation under wrong keys, severity-dependent drift to "German" as a language judge; the Phase-C judge's robustness is the curriculum, not the architecture); `scripts/train.py --phase phase_b --init-from`. `diff_voyn/model/` holds the MDLM diffusion core (`diffusion.py`, SUBS + Rao-Blackwellized NELBO, verified against exact enumerated references in `tests/test_diffusion.py`) and the RMSNorm/SwiGLU/RoPE backbone (`backbone.py`, presets via `model_preset()` in `infra/config.py`). Training runner: `scripts/train.py` (`--phase pilot|phase_a --model 25m|85m`, `--resume`); checkpoint scoring harness: `scripts/score_checkpoint.py`. The `diff_voyn/` package also holds the Phase-0 deliverables: frozen vocab (`vocab.py`, 32 symbols — the 23 Naibbe letters resolved from Greshko's actual tables, plus k/w), shared normalizer (`normalize.py`), corpus assembly + held-out splits (`corpus/`), data loader with masking sampler (`data/`), training infra with ClearML/EMA/checkpointing (`infra/`), pinned cipher wrappers (`ciphers/`), and VMS ingest (`vms/`). Phase-0 decisions and acceptance numbers are recorded in `docs/phase0_decisions.md`; **read that plus the docs below before changing anything Phase 0 froze** (vocab, normalizer, splits are versioned — bump the version rather than mutating v1).

Key scripts (all `uv run python scripts/<name>.py`): `fetch_external.py` (idempotent clone/pin of greshko/naibbe-cipher @ df3d074 and alexanderboxer/voynich-attack @ e324bee, plus voynich.nu downloads), `build_corpora.py` (assemble + splits), `tune_ciphers.py` (0.7 acceptance), `ingest_vms.py`, `g0_check.py` (gate verification; logs random-init NELBO to ClearML).

Data layout under `/workspace/data` (gitignored bind mount): `external/` (pinned repos — SHA verified at import), `raw/` (italian texts + voynich.nu files), `corpora/v1/` (normalized docs, `manifest.json` corpus table, `splits_v1.json`), `ciphers/` (tuned pseudo-VMS tables + acceptance stats), `vms/` (per-dialect Currier A/B streams).

The design is fully specified in `reference_docs/` — read those before implementing anything. They are the pre-registration record and are left byte-identical: the abstract's headline ("Germanic candidates receive the highest likelihood") was tested and is **not** supported, and design §8's gradient outer tier was replaced by discrete polish + MDL ranking — see `docs/project_status.md` §5 and `docs/project_goals_and_progress.md` §6–7.

- `A Diffusion-Based Framework for Language Identification of Voynich-Like Ciphertext.md` — the paper abstract / framing.
- `Design and Training of the Multilingual Diffusion Backbone.md` — the authoritative design doc: every architecture/training decision with alternatives and reasoning (referenced below as "design §N").
- `Diffusion Model Training - Task Breakdown.md` — the execution plan: Phases 0–6 with hard gates G0–G5, task IDs (e.g. 0.1, 5.4), priorities, and acceptance criteria. New work should map to a task ID from this plan.

## Polygraphia / Trithemius quarantine (2026-08-31)

The Polygraphia "Ave Maria" digitization (`docs/polygraphia_status.md`) is **instrumentation only and is quarantined from every control-set workflow**: never add Polygraphia/Trithemius instances to a control battery, a wordhom/Phase-6 control manifest, an abstention-threshold calibration set, or any aggregate "controls" statistic. Reason: the provisional 41-column cyclic cells produced the pipeline's first false CALLS (periodic pseudo-language objective trap, margins 1.76–1.96 at SER 0.74–0.77), the cipher is not representable in the wordhom key space (word→letter collisions grow with table size: 73/889 words at 41 cols, 275/1746 at 92), and the transcription is unvalidated single-key for most columns. The findings stand as recorded (status doc + `DATA_ROOT/analysis/polygraphia_anchor/`); the anchor may be revisited only as its own explicitly-named study, and joining any control set would require the user's say-so.

## Solver selection convention

When the user asks to run "the wordhom solver" (or a solve of word-homophonic cells generally), they mean the **most advanced solving pipeline currently available** — as of 2026-08-31 that is the hapax-wildcard objective with wildcard→anneal re-admission and the post-all loop (`docs/alt_loop_plan.md` §8.4–8.6, §10 / `scripts/altloop_vms.py --wild --wild-anneal`), not plain multi-restart SA (`WordHomophonicHead.solve`). Plain SA is a baseline/control, to be run only when the user asks for a simpler version explicitly or when a study's design needs it as a comparison arm. If it is unclear which solver applies, or the most powerful one seems inappropriate for the task (e.g. wrong instance shape, prohibitive cost, a frozen-protocol replication), ask the user instead of silently downgrading.

## Documentation coherence enforcement (2026-09-01)

`scripts/doc_coherence_check.py` runs at every Stop (blocks the turn until clean), at SessionStart, and as the git pre-commit hook. It fails when a `docs/*.md` lacks its `Record status (banner …)` paragraph, when a known-superseded phrase appears without its qualifier (`PHRASE_RULES` in the script — extend it whenever a study supersedes a claim), when a backticked `docs/…` reference is dangling, when a memory is not indexed exactly once, or when docs/memory changed but `docs/project_status.md` was not touched (bump its `Status date`, update §1/§2/§5, or add a "(reviewed, no change)" note). After a study that changes a result, solver, judge or open item, run the `/coherence-review` skill (semantic pass) — the checker cannot see that a new number supersedes an old one.

## Environment and commands

Development happens inside a devcontainer (CPU or GPU variant, selected via `.devcontainer/cpu/` or `.devcontainer/gpu/`; the GPU one uses a PyTorch CUDA base image and `runtime: nvidia`). Dependencies are managed with **uv** (Python 3.12, venv at `/workspace/.venv`):

```bash
uv sync                 # base deps + dev group (black, isort, ruff, pytest)
uv sync --group gpu     # additionally install the full GPU/training stack (torch, lightning, clearml, ...)

uv run pytest                                   # run tests
uv run pytest path/to/test_file.py::test_name   # run a single test
uv run black . && uv run isort .                # format (black is pinned to 24.8.0)
uv run ruff check .                             # lint
```

The `DEVICE` env var (`cpu`/`cuda`) selects which dependency set `post-create.sh` installs.

**You (Claude Code) are running inside the GPU devcontainer.** Two persistence rules follow:

- **All data, results, model checkpoints, logs, and downloaded models go under `/workspace/data`** (a gitignored bind mount from the host, `DATA_DIR` in `.devcontainer/.env`; HF models via `HF_HOME=/workspace/data/hf-models`). Anything written elsewhere in the container filesystem outside `/workspace` is lost on rebuild, and large artifacts must never land in git.
- **Never install packages ad hoc** (`pip install`, bare `uv pip install`) — the environment is recreated from spec on every container rebuild. Record every addition where it persists: Python deps via `uv add <pkg>` (or `uv add --group gpu` / `--group dev` for training-only or tooling deps) so `pyproject.toml` and `uv.lock` are updated and committed; system-level packages in `.devcontainer/Dockerfile`; setup steps in `.devcontainer/post-create.sh`.

Experiment tracking uses **ClearML** (self-hosted at `clearml.acet.network`, credentials passed through from the host environment). Every training run gets a ClearML Task capturing config, per-language NELBO scalars, and checkpoint artifacts.

## Architecture (from the design doc — the "big picture")

One shared backbone, three consumers: (a) per-language ELBO as the likelihood metric, (b) a language-ID head, (c) a differentiable plaintext evaluator for cipher decoding heads.

- **Model**: encoder-only transformer (RMSNorm, SwiGLU, RoPE), ~85M (12L/d768) plus a ~25M sibling for cheap restart-heavy search loops. MDLM-style continuous-time masked diffusion (SUBS parameterization, Rao-Blackwellized NELBO), no time-conditioning network. Context 1024 chars.
- **Tokenization**: character-level only, ~32-symbol vocab (normalized Latin alphabet + MASK/NULL/BOS/EOS). **No subwords, and no SPACE token — all whitespace is stripped from every text stream (corpora, ciphertexts, VMS) in preprocessing.** This is a modeling decision (design §2), not a convenience.
- **Languages**: Latin, Italian, German (frozen inventory, task 0.2), all trained jointly from step one via an additive per-position language embedding with 10% conditioning dropout. Never introduce languages sequentially — symmetry across languages is a correctness property of the instrument.
- **Cipher heads** (Phase 5): optimized against a **frozen (EMA) backbone**, validated in strict difficulty order: 1:1 substitution → unigram homophonic → Naibbe mixed unigram-bigram (`naibbe_v2.py`, greshko/naibbe-cipher @ `df3d074`) → arithmetic sum-to-target (`voynpy.pseudo_vms` from Boxer's voynich-attack repo). Heads feed the backbone expected embeddings on a 2N-slot frame with NULL blending; inner search uses a cheap n-gram DP, the diffusion ELBO scores shortlists.

## Non-negotiables the plan is built around

These come from the design doc's load-bearing decisions; violating them silently biases the scientific result:

1. **Bound-tightness fairness (R1)**: language ranking compares per-language ELBOs, so anything that tightens one language's bound preferentially (sequential training, per-language tokenizer fit, lossy alphabet mapping) is a bias. Calibration offsets (task 3.4) are measured, versioned, and applied in exactly one place.
2. **Gates are ordered**: LID head attaches only after the backbone stabilizes (stop-gradient first, then joint with small λ); the evaluator is frozen before any cipher-head optimization; G3 (calibrated metric) must pass before G4.
3. **Per-language held-out NELBO is the canary** at every gate — if it degrades or a language stalls, adjust sampling temperature or λ, don't change the schedule.
4. **Scoring uses common random numbers** across language conditions (same masking realizations) — the ranking consumes score *differences*, and CRN is the main variance-reduction lever.
5. Clean-text fraction is retained in every training phase (calibration is defined on clean text); Currier A and B dialects of the VMS are never pooled.
6. Known numerical trap: `logaddexp(−∞,−∞)` NaN when log-space blending NULL slots — keep the smoke test (design §8).
